// require('angular')
// require(['angular', 'configuration/index'])
// require('routes')

// define(['require', 'angular', 'app', 'configuration', 'common'], function(require) {

//     var angular = require('angular');
//     // require('configuration');
//     // require('common');
    var app = require('app');

    describe('just checking', function() {

        it('works for app', function() {
            console.log(app);
            // expect(el.text()).toEqual('require.js up and running');
        });
    });

// });